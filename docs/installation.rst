============
Installation
============

At the command line::

    $ easy_install django-livemap

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-livemap
    $ pip install django-livemap
